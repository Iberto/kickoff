# Database
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# App
ENVIRONMENT=development
SECRET_KEY=

# CORS — comma-separated list of allowed origins
# ALLOWED_ORIGINS must be overridden before any non-local deployment
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173"]

